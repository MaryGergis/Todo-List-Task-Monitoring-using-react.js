import React from 'react';

const NotFoundPage = (props) => {
    return (
        <div style={{marginTop:'12%'}}>
            <img src={require('../Images/NotFoundPage.jpeg')} width={'50%'} style={{marginLeft:'25%'}} alt='not found page'/> 
        </div>
    );
};

export default NotFoundPage;